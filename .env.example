# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Optional explainer-only service. The computation engine still solves and
# verifies results; Groq only rewrites final answers for readability.
EXPLAINER_PROVIDER="groq"
GROQ_API_KEY="MY_GROQ_API_KEY"
GROQ_MODEL="llama-3.3-70b-versatile"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
# APP_URL="MY_APP_URL"

# For deployment (Render URL for backend)
# When you deploy the 'backend/' folder to Render, put the URL here.
VITE_BACKEND_URL="https://your-backend-render-url.com"

# NODE_ENV: Set to 'production' for build and deployment.
# PORT: The server port (3000 for AI Studio Preview).
NODE_ENV="development"
PORT=3000
